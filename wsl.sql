create database portal_data;
use portal_data; 
create table [user_data] (personid int(50) not null auto_increment primary key,email varchar(50),firstname varchar(35),lastname varchar(50),password varchar(50));